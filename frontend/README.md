# Frontend (React Todo SPA)

Features:
- Add / edit / delete todos
- Mark complete / incomplete
- Filter: All / Active / Completed
- Toggle all (visible) and clear completed
- Persistent storage via `localStorage`

## Run (dev)
```bash
npm install
npm run dev
```

The dev server uses `REACT_APP_PORT` (from `.env`) if available.
